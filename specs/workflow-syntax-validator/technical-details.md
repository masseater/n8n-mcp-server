# 技術詳細ドキュメント

## 既存実装分析（czlonkowski/n8n-mcp）

### 🔍 発見された高度なValidation実装

**czlonkowski/n8n-mcp**（9.5k stars）が既に包括的なvalidation実装を提供：

#### 実装済みValidationツール
1. **`validate_workflow`** - 包括的ワークフロー検証（構造、接続、式、AIツール）
2. **`validate_node_minimal`** - 必須フィールドの高速チェック
3. **`validate_node_operation`** - ノード設定の詳細検証
4. **`validate_workflow_connections`** - 接続整合性チェック
5. **`validate_workflow_expressions`** - 式構文検証

#### WorkflowValidatorクラスの機能
```typescript
// 4つの検証プロファイル
profile: 'minimal' | 'runtime' | 'ai-friendly' | 'strict'

// 包括的検証機能
- validateWorkflowStructure: 基本構造検証
- validateAllNodes: 全ノード検証
- validateConnections: 接続検証（循環参照、孤立ノード検出）
- validateExpressions: 式構文検証
- checkWorkflowPatterns: ベストプラクティスチェック
- validateAISpecificNodes: AI特化ノード検証
```

#### 検証結果構造
```typescript
interface WorkflowValidationResult {
  valid: boolean;
  errors: ValidationIssue[];
  warnings: ValidationIssue[];
  statistics: {
    totalNodes: number;
    enabledNodes: number;
    triggerNodes: number;
    validConnections: number;
    invalidConnections: number;
    expressionsValidated: number;
  };
  suggestions: string[];
}
```

#### パフォーマンス
- **541ノードタイプ100%カバー**
- **平均レスポンス12ms**
- **処理時間100-500ms**（ワークフローサイズ依存）

## アーキテクチャ
### システム構成図
```
┌─────────────────────────────────────────────────────────┐
│                MCP Server                               │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────────────────────┐│
│  │ Tool Registry   │  │    Workflow Validator Tools     ││
│  │                 │  │                                 ││
│  │ - register()    │  │ ┌─────────────────────────────┐ ││
│  │ - getTool()     │  │ │   ValidateWorkflowTool      │ ││
│  │                 │  │ │                             │ ││
│  └─────────────────┘  │ │ - executeCore()             │ ││
│                       │ │ - formatResponse()          │ ││
│  ┌─────────────────┐  │ └─────────────────────────────┘ ││
│  │ Error Handler   │  │                                 ││
│  │                 │  │ ┌─────────────────────────────┐ ││
│  │ - handleError() │  │ │ ValidateWorkflowFromFileTool│ ││
│  │ - formatError() │  │ │                             │ ││
│  │                 │  │ │ - executeCore()             │ ││
│  └─────────────────┘  │ │ - formatResponse()          │ ││
│                       │ └─────────────────────────────┘ ││
│                       └─────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────┐
│            Validation Engine                            │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────────────────────┐│
│  │ Schema Manager  │  │      Validation Pipeline       ││
│  │                 │  │                                 ││
│  │ - baseSchema    │  │ ┌─────────────────────────────┐ ││
│  │ - nodeSchema    │  │ │    BasicValidator           │ ││
│  │ - connSchema    │  │ │ - validateStructure()       │ ││
│  │ - extendSchema()│  │ │ - validateRequired()        │ ││
│  │                 │  │ └─────────────────────────────┘ ││
│  └─────────────────┘  │                                 ││
│                       │ ┌─────────────────────────────┐ ││
│  ┌─────────────────┐  │ │    NodeValidator            │ ││
│  │ Error Collector │  │ │ - validateNodes()           │ ││
│  │                 │  │ │ - checkDuplicates()         │ ││
│  │ - addError()    │  │ │ - validateTypes()           │ ││
│  │ - addWarning()  │  │ └─────────────────────────────┘ ││
│  │ - getResults()  │  │                                 ││
│  │                 │  │ ┌─────────────────────────────┐ ││
│  └─────────────────┘  │ │  ConnectionValidator        │ ││
│                       │ │ - validateConnections()     │ ││
│                       │ │ - checkCircular()           │ ││
│                       │ │ - validateReferences()      │ ││
│                       │ └─────────────────────────────┘ ││
│                       └─────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────┐
│              Support Components                         │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────────────────────┐│
│  │  File Loader    │  │      Error Reporter             ││
│  │                 │  │                                 ││
│  │ - loadFromFile()│  │ ┌─────────────────────────────┐ ││
│  │ - validateJSON()│  │ │   MessageFormatter          │ ││
│  │ - handleErrors()│  │ │ - formatError()             │ ││
│  │                 │  │ │ - formatWarning()           │ ││
│  └─────────────────┘  │ │ - generateSuggestions()     │ ││
│                       │ └─────────────────────────────┘ ││
│  ┌─────────────────┐  │                                 ││
│  │ Node Type Def   │  │ ┌─────────────────────────────┐ ││
│  │                 │  │ │   Statistics Calculator     │ ││
│  │ - getNodeDef()  │  │ │ - calculateStats()          │ ││
│  │ - validateType()│  │ │ - generateSummary()         │ ││
│  │ - getRules()    │  │ └─────────────────────────────┘ ││
│  │                 │  └─────────────────────────────────┘│
│  └─────────────────┘                                     │
└─────────────────────────────────────────────────────────┘
```

### 技術スタック
- **言語**: TypeScript 5.x
- **ランタイム**: Node.js LTS 22.10.0
- **バリデーション**: Zod (^3.x)
- **テスト**: Vitest
- **ビルド**: TypeScript Compiler
- **リンター**: ESLint + Prettier
- **MCPプロトコル**: @modelcontextprotocol/sdk

### インフラ構成
- **実行環境**: 既存のn8n MCP serverプロセス内
- **ファイルシステム**: ローカルファイルアクセス（読み取り専用）
- **メモリ**: 共有メモリ空間（他のMCPツールと共存）
- **ネットワーク**: 不要（ローカル処理のみ）

## 技術選定
### 採用技術とその理由

#### 1. Zod バリデーションライブラリ
**採用理由**:
- 既存コードベースで使用済み（一貫性）
- TypeScriptとの優れた統合
- 複雑なバリデーションルールの表現が可能
- エラーメッセージのカスタマイズが容易
- **調査結果により確認**: refine()、transform()、superRefine()、extend()の全機能が利用可能
- **実証済み**: 既存スキーマとの100%互換性を保持しつつ拡張可能

**代替案との比較**:
- **Joi**: より多機能だが、TypeScript統合が劣る
- **Yup**: シンプルだが、複雑なバリデーションに制限
- **AJV**: 高速だが、TypeScript統合とエラーメッセージが劣る

**拡張機能の実装可能性（調査結果）**:
- ノードタイプ固有バリデーション: ✅ 実装可能
- 複雑な接続パターンチェック: ✅ superRefine()で実装
- 循環参照検出: ✅ カスタムロジックで実装
- エラーメッセージのカスタマイズ: ✅ 完全対応
- 修正提案の自動生成: ✅ 実装可能

#### 2. RawTool パターン
**採用理由**:
- 既存のMCPツールと一貫したアーキテクチャ
- `raw`パラメータによるレスポンス制御
- エラーハンドリングの統一化
- テスト容易性

#### 3. ファイルローダーの再利用
**採用理由**:
- 既存の`loadWorkflowFromFile`関数の実績
- 一貫したエラーハンドリング
- ファイルアクセスのセキュリティ制御

### 代替案との比較
| 技術要素 | 採用案 | 代替案A | 代替案B | 選定理由 |
|---------|--------|---------|---------|----------|
| バリデーション | Zod | Joi | AJV | 既存コードとの一貫性 |
| アーキテクチャ | RawTool | BaseTool | 独自実装 | 既存パターンとの統合 |
| エラー処理 | 既存Error継承 | 独自Error | 標準Error | 一貫したエラーハンドリング |

## データ設計
### データモデル
#### ワークフロー定義（入力）
```typescript
interface WorkflowDefinition {
  name: string;
  active: boolean;
  nodes: INode[];
  connections: IConnections;
  settings?: IWorkflowSettings;
  tags?: string[];
}

interface INode {
  id: string;
  name: string;
  type: string;
  typeVersion: number;
  position: [number, number];
  parameters: INodeParameters;
  credentials?: INodeCredentials;
  disabled?: boolean;
  // 他のオプション項目...
}
```

#### バリデーション結果（出力）
```typescript
interface ValidationResult {
  valid: boolean;
  errors: ValidationError[];
  warnings: ValidationWarning[];
  statistics: ValidationStatistics;
}

interface ValidationError {
  code: string;
  message: string;
  path: string;
  severity: 'error' | 'warning' | 'info';
  nodeId?: string;
  suggestions?: string[];
}

interface ValidationStatistics {
  totalNodes: number;
  totalConnections: number;
  errorCount: number;
  warningCount: number;
  validationTime: number;
}
```

### データベーススキーマ
**該当なし**: バリデーション結果は永続化せず、メモリ内処理のみ

### データフロー
```
Input (JSON/File) → Parse → Basic Validation → Node Validation →
Connection Validation → Advanced Validation → Error Collection →
Format Response → Output (ValidationResult)
```

## API設計
### エンドポイント一覧

#### 1. validate_workflow
```typescript
// MCPツール定義
{
  name: "validate_workflow",
  description: "n8nワークフローの構文を検証する",
  inputSchema: {
    type: "object",
    properties: {
      workflow: {
        type: "object",
        description: "検証するワークフロー定義"
      },
      strict: {
        type: "boolean",
        description: "厳格モード（警告もエラーとして扱う）",
        default: false
      },
      includeWarnings: {
        type: "boolean",
        description: "警告を結果に含める",
        default: true
      },
      raw: {
        type: "boolean",
        description: "詳細な結果を返す",
        default: false
      }
    },
    required: ["workflow"]
  }
}
```

#### 2. validate_workflow_from_file
```typescript
// MCPツール定義
{
  name: "validate_workflow_from_file",
  description: "ファイルからワークフローを読み込んで構文を検証する",
  inputSchema: {
    type: "object",
    properties: {
      filePath: {
        type: "string",
        description: "ワークフローJSONファイルのパス"
      },
      strict: {
        type: "boolean",
        description: "厳格モード",
        default: false
      },
      includeWarnings: {
        type: "boolean",
        description: "警告を結果に含める",
        default: true
      },
      raw: {
        type: "boolean",
        description: "詳細な結果を返す",
        default: false
      }
    },
    required: ["filePath"]
  }
}
```

### リクエスト/レスポンス仕様

#### 成功レスポンス（raw=false）
```json
{
  "success": true,
  "message": "ワークフローの検証が完了しました",
  "data": {
    "valid": true,
    "errorCount": 0,
    "warningCount": 2,
    "nodeCount": 5,
    "validationTime": 45
  }
}
```

#### 成功レスポンス（raw=true）
```json
{
  "success": true,
  "message": "ワークフローの検証が完了しました",
  "data": {
    "valid": false,
    "errors": [
      {
        "code": "MISSING_REQUIRED_FIELD",
        "message": "ノード 'HTTP Request' に必須パラメータ 'url' がありません",
        "path": "nodes[1].parameters.url",
        "severity": "error",
        "nodeId": "node-123",
        "suggestions": [
          "parametersオブジェクトにurlフィールドを追加してください",
          "例: \"url\": \"https://api.example.com\""
        ]
      }
    ],
    "warnings": [
      {
        "code": "DUPLICATE_NODE_NAME",
        "message": "ノード名 'HTTP Request' が重複しています",
        "path": "nodes[1].name",
        "severity": "warning",
        "nodeId": "node-123",
        "suggestions": [
          "ノード名を一意にしてください",
          "例: 'HTTP Request 1', 'HTTP Request 2'"
        ]
      }
    ],
    "statistics": {
      "totalNodes": 5,
      "totalConnections": 4,
      "errorCount": 1,
      "warningCount": 1,
      "validationTime": 45
    }
  }
}
```

#### エラーレスポンス
```json
{
  "success": false,
  "message": "ワークフローの検証中にエラーが発生しました: Invalid JSON",
  "data": null,
  "isError": true
}
```

### 認証・認可
- **認証**: MCPサーバーレベルで実装済み（本機能では追加認証不要）
- **認可**: ファイルアクセス権限のチェック（既存のfile-loader機能を使用）

## セキュリティ
### セキュリティ要件
1. **入力データ検証**
   - JSONペイロードサイズ制限（最大10MB）
   - ネストレベル制限（最大50階層）
   - 文字列長制限（フィールドごと）

2. **ファイルアクセス制御**
   - パストラバーサル攻撃の防止
   - 読み取り専用アクセス
   - 許可されたディレクトリ外へのアクセス禁止

3. **リソース制限**
   - 処理時間上限（30秒）
   - メモリ使用量制限（100MB）
   - 同時処理数制限（設定可能）

### 実装方針
#### 入力サニタイゼーション
```typescript
// JSONペイロードサイズチェック
function validatePayloadSize(data: string): void {
  if (data.length > MAX_PAYLOAD_SIZE) {
    throw new ValidationError('Payload too large');
  }
}

// ネストレベルチェック
function validateNestingDepth(obj: any, depth = 0): void {
  if (depth > MAX_NESTING_DEPTH) {
    throw new ValidationError('Nesting too deep');
  }
  // 再帰的チェック...
}
```

#### ファイルアクセス制御
```typescript
// パストラバーサル防止
function validateFilePath(filePath: string): string {
  const resolved = path.resolve(filePath);
  const allowed = path.resolve(ALLOWED_BASE_PATH);

  if (!resolved.startsWith(allowed)) {
    throw new FileError('Access denied');
  }

  return resolved;
}
```

#### リソース制限
```typescript
// 処理時間制限
function withTimeout<T>(promise: Promise<T>, timeout: number): Promise<T> {
  return Promise.race([
    promise,
    new Promise<never>((_, reject) =>
      setTimeout(() => reject(new Error('Timeout')), timeout)
    )
  ]);
}
```

## パフォーマンス要件
### レスポンスタイム目標
- **小規模ワークフロー（<10ノード）**: 100ms以内
- **中規模ワークフロー（10-50ノード）**: 500ms以内
- **大規模ワークフロー（>50ノード）**: **調査項目3で決定**

### 最適化手法
#### 1. 並列バリデーション
```typescript
// ノードバリデーションの並列実行
async function validateNodesInParallel(nodes: INode[]): Promise<ValidationError[]> {
  const validationPromises = nodes.map(node => validateSingleNode(node));
  const results = await Promise.allSettled(validationPromises);
  return results.flatMap(result =>
    result.status === 'fulfilled' ? result.value : []
  );
}
```

#### 2. 早期終了最適化
```typescript
// 重要なエラーで早期終了
function validateWithEarlyExit(workflow: WorkflowDefinition): ValidationResult {
  const criticalErrors = validateCriticalStructure(workflow);
  if (criticalErrors.length > 0) {
    return { valid: false, errors: criticalErrors, warnings: [] };
  }

  // 詳細バリデーション続行...
}
```

#### 3. メモリ効率化
```typescript
// ストリーミング処理でメモリ使用量削減
function* validateNodesStream(nodes: INode[]): Generator<ValidationError> {
  for (const node of nodes) {
    yield* validateNodeErrors(node);
  }
}
```

### スループット目標
- **同時処理**: **調査項目3で決定**
- **メモリ効率**: ワークフローあたり**調査項目3で決定**

## 開発環境
### 必要なツール
- **Node.js**: LTS 22.10.0以上
- **npm/pnpm**: パッケージ管理
- **TypeScript**: 5.x
- **Vitest**: テストランナー
- **ESLint + Prettier**: コード品質

### セットアップ手順
1. **依存関係インストール**
   ```bash
   pnpm install
   ```

2. **開発用ビルド**
   ```bash
   pnpm run build:dev
   ```

3. **テスト実行**
   ```bash
   pnpm run test
   pnpm run test:coverage
   ```

4. **リンター実行**
   ```bash
   pnpm run lint
   pnpm run format
   ```

### デバッグ環境
```typescript
// デバッグ用設定
const DEBUG_CONFIG = {
  verbose: true,
  logLevel: 'debug',
  enableProfiling: true,
  dumpValidationSteps: true
};
```

## テスト戦略
### テストの種類
#### 1. 単体テスト
- **対象**: 各バリデーション関数
- **フレームワーク**: Vitest
- **カバレッジ目標**: 90%以上

#### 2. 統合テスト
- **対象**: MCPツール全体の動作
- **テストケース**:
  - 正常なワークフローの検証
  - 各種エラーパターンの検証
  - ファイル入力の検証

#### 3. パフォーマンステスト
- **対象**: 大規模ワークフローの処理性能
- **測定項目**: レスポンスタイム、メモリ使用量
- **ベンチマーク**: 様々なサイズのワークフロー

### カバレッジ目標
- **行カバレッジ**: 90%以上
- **分岐カバレッジ**: 85%以上
- **関数カバレッジ**: 95%以上

### テストデータ設計
#### 正常系テストケース
```typescript
const VALID_WORKFLOWS = {
  minimal: {
    name: "Minimal Workflow",
    nodes: [startNode],
    connections: {}
  },
  complex: {
    name: "Complex Workflow",
    nodes: [startNode, httpNode, ifNode, endNode],
    connections: { /* 複雑な接続パターン */ }
  }
};
```

#### 異常系テストケース
```typescript
const INVALID_WORKFLOWS = {
  missingName: {
    nodes: [],
    connections: {}
    // name フィールドなし
  },
  duplicateIds: {
    name: "Duplicate IDs",
    nodes: [
      { id: "same-id", /* ... */ },
      { id: "same-id", /* ... */ }
    ],
    connections: {}
  },
  circularReference: {
    // 循環参照を含む接続
  }
};
```

### 継続的テスト
- **CI/CD統合**: GitHub Actionsでの自動テスト
- **リグレッションテスト**: 既存機能への影響チェック
- **パフォーマンス監視**: ベンチマーク結果の継続的監視
